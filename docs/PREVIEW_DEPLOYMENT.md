# Preview Deployment

This marker file triggers a Vercel Preview deployment for the `refactor/catalog-foundation` branch. It does not change application behavior, botanical data, images, environment configuration, or production deployment settings.
